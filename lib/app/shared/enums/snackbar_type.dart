/// Visual style used when showing a snackbar.
///
/// [normal] renders the message instantly; [animated] types it out character by
/// character (used for the download/progress style messages).
enum SnackbarType {
  normal,
  animated,
}
